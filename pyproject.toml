[project]
name = "deye-power-limit"
version = "1.0.0"
description = "Set power output limit on Deye microinverters via Solarman V5 / Modbus"
license = "MIT"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
